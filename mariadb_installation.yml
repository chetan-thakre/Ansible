---
  - name: ansible playbook to install mariadb database
    hosts: webserver
    become: true 
    tasks:
      - name: install mariadb-server package
        yum:
          name: mariadb-server
          state: latest 
